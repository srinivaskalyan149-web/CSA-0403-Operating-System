#include<stdio.h>
int main(){
    int b[5]={100,500,200,300,600};
    int p,best=9999,pos=-1,i;
    scanf("%d",&p);
    for(i=0;i<5;i++)
        if(b[i]>=p && b[i]<best)
            best=b[i],pos=i;
    if(pos!=-1)
        printf("Allocated in Block %d",pos+1);
}